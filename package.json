{
  "name": "hello-prisma",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "find": "ts-node-dev --respawn --transpile-only src/find.ts",
    "create": "ts-node-dev --respawn --transpile-only src/create.ts",
    "update": "ts-node-dev --respawn --transpile-only src/update.ts",
    "delete": "ts-node-dev --respawn --transpile-only src/delete.ts",
    "pagination": "ts-node-dev --respawn --transpile-only src/pagination-sorting.ts",
    "rela": "ts-node-dev --respawn --transpile-only src/relational-queries.ts",
    "filter": "ts-node-dev --respawn --transpile-only src/filtering.ts",
    "logging": "ts-node-dev --respawn --transpile-only src/logging.ts",
    "aggregates": "ts-node-dev --respawn --transpile-only src/aggregates.ts",
    "groupBy": "ts-node-dev --respawn --transpile-only src/groupBy.ts",
    "batch": "ts-node-dev --respawn --transpile-only src/batch.ts",
    "interactive": "ts-node-dev --respawn --transpile-only src/interactive.ts"
  },
  "devDependencies": {
    "@types/node": "^22.13.13",
    "prisma": "^6.5.0",
    "tsx": "^4.19.3",
    "typescript": "^5.8.2"
  },
  "dependencies": {
    "@prisma/client": "^6.5.0",
    "ts-node-dev": "^2.0.0"
  }
}
